<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="TarotDesk.ReadingDetailPage"
    Title="Chi tiết lưới trải"
    BackgroundColor="#1a0a2e">

    <ContentPage.Background>
        <StaticResource Key="StarfieldBrush" />
    </ContentPage.Background>

    <VerticalStackLayout Padding="15" Spacing="10">

        <!-- Header -->
        <VerticalStackLayout Spacing="5">
            <Label 
                x:Name="ReadingNameLabel"
                FontAttributes="Bold"
                TextColor="#FFFFFF"
                FontSize="20" />

            <Label 
                x:Name="ReadingDateLabel"
                TextColor="#FFFFFF"
                FontSize="12" />
        </VerticalStackLayout>

        <!-- Divider -->
        <BoxView 
            HeightRequest="1"
            Color="#512DA850" />

        <!-- Cards List -->
        <Label 
            Text="◆ Các lá bài:"
            TextColor="#FFFFFF"
            FontSize="14"
            FontAttributes="Bold" />

        <CollectionView 
            x:Name="CardsCollectionView">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout 
                    Orientation="Vertical"
                    ItemSpacing="10" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border 
                        Padding="15"
                        BackgroundColor="#2D1B4E80"
                        Stroke="#FFFFFF20"
                        StrokeThickness="1">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="15" />
                        </Border.StrokeShape>
                        <VerticalStackLayout 
                            Spacing="10">

                        <Image 
                            Source="{Binding ImagePath}"
                            HeightRequest="150"
                            Aspect="AspectFit" />

                        <Label 
                            Text="{Binding Name}"
                            FontAttributes="Bold"
                            TextColor="#FFFFFF"
                            FontSize="16" />

                        <Label 
                            Text="{Binding Meaning}"
                            TextColor="#FFFFFF"
                            FontSize="13"
                            LineBreakMode="WordWrap" />
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>

</ContentPage>
